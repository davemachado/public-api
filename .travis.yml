sudo: required
language: go
go: 1.8
services:
    - docker

script:
    - wget https://raw.githubusercontent.com/toddmotto/public-apis/master/json/entries.min.json
    - make && make login && make push
